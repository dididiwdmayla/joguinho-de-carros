{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build && echo '--- PWD ---' && pwd && echo '--- LS ROOT ---' && ls -la && echo '--- LS DIST ---' && ls -la dist && echo '--- LS DIST/ASSETS ---' && ls -la dist/assets",
  "installCommand": "npm install",
  "outputDirectory": "dist"
}
