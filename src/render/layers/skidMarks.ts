/** Skid marks layer: rubber left by sliding tyres. Empty for now. */
import type { RenderContext } from '../scene'

export function drawSkidMarks(_context: RenderContext): void {
  // No skid marks in this stage.
}
