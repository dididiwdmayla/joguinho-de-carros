/** Props layer: parked cars, cones, barriers, planters. Empty for now. */
import type { RenderContext } from '../scene'

export function drawProps(_context: RenderContext): void {
  // No props in this stage.
}
