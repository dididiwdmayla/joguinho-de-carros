/** Effects layer: dust, smoke, particles. Empty for now. */
import type { RenderContext } from '../scene'

export function drawEffects(_context: RenderContext): void {
  // No effects in this stage.
}
