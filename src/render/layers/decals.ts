/** Decals layer: oil stains, cracks, manholes painted on the asphalt. Empty for now. */
import type { RenderContext } from '../scene'

export function drawDecals(_context: RenderContext): void {
  // No decals in this stage.
}
