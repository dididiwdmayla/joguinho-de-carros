{
  "name": "joguinho-de-carros",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import ./scripts/ts-resolve.mjs src/vehicle/physics.test.ts && node --import ./scripts/ts-resolve.mjs src/level/level.test.ts",
    "build": "tsc --noEmit && vite build"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.6"
  }
}
